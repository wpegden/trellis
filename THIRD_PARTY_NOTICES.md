# Third-Party Notices

Trellis includes or depends on third-party software packages that remain under their own licenses.

This repository's PolyForm Noncommercial License 1.0.0 applies only to project-authored Trellis files unless otherwise noted. It does not relicense third-party dependencies.

## Rust dependencies

See `kernel/Cargo.lock` for the exact resolved Rust dependency versions.

## Node/viewer dependencies

See `viewer/package-lock.json` for the exact resolved Node dependency versions.

The viewer currently depends directly on:

- `express`
- `compression`

The viewer lockfile records additional transitive dependencies and their license metadata.
