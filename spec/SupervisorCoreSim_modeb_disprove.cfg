SPECIFICATION Spec

CHECK_DEADLOCK
    FALSE

\* DISPROVE-CLOSE BAIT (expected-FAIL, NOT in the green suite).  A milestone
\* witness that does NOT require reaching full Done: a violation of
\* C2NeverDisproveClosed witnesses a reachable state where target c2 — whose
\* live side started "prove" (a FALSE side) — has been flipped by the audit
\* lane to "disprove" and then CLOSED on that side.  This is the load-bearing
\* prove-or-disprove soundness-symmetry exercise: the disprove side closes
\* through the SAME axiom-floor gate, and only after the audit flip.  Shallower
\* than full Done, so a random sim reaches it readily.
INVARIANTS
    C2NeverDisproveClosed

\* Invoke under -simulate (sim-only):
\*   java -jar tla2tools.jar -simulate num=5000000 -depth 80 -seed S \
\*       -config SupervisorCoreSim_modeb_disprove.cfg SupervisorCoreSim_modeb_reach.tla
