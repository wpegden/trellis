SPECIFICATION Spec

\* Deadlocks at MaxCycle are expected (the cycle counter saturates), same
\* as SupervisorCoreSim.cfg — a bound-induced terminal, not a real deadlock.
CHECK_DEADLOCK
    FALSE

INVARIANTS
    TypeOK
    CrossTargetFormalIsolation

\* Phase IV step 12: 2 nodes + 2 backends available + a real formal-import
\* edge n1 -> n2, but a UNIFORM nodeTarget (every node = TabletTarget).
\* CrossTargetFormalIsolation must HOLD. Run in -simulate mode (2 nodes can
\* OOM exhaustive BFS). The non-uniform VIOLATION is the throwaway
\* SupervisorCoreSim_isolation_bugcheck, expected-fail and NOT in the green
\* suite.
