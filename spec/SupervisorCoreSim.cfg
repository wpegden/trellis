SPECIFICATION Spec

\* Deadlocks at MaxCycle are expected (cycle counter saturates and
\* StartCycle's `cycle < MaxCycle` precondition disables further
\* progress).  This is a bound-induced terminal, not a real protocol
\* deadlock.
CHECK_DEADLOCK
    FALSE

INVARIANTS
    TypeOK
    HumanGateMatchesState
    NeedInputReentryGuarded
    InFlightKindMatchesStage
    CleanupBlockersAreAdmissible
    NoAdvancePhaseWithBlockers
    CompleteRequiresChallengeCoverage
    CleanupDoneTerminal
    StalePassClosurePreventsCleanupAdvance
    CrossTargetFormalIsolation
    CoarseAnchorSafe
    AnchorChangeForbiddenDuringGlobalRepair
    LocalModeSoundnessCarveOut
    AuthorizedNodesScopeContract
    PendingTaskStaging
    PhaseDormancyContract
    QuiescentLiveEqualsCommitted
    GlobalBlockersExhaustive
    ReviewerScopeAuthorizationComplete
    GlobalRepairLifecycle
    SingleAuditAtATime
    AuditStageConsistency
    GapLoopTerminates
    GapStageRejectConsistency
    SidecarQueueSubsetOpen
    \* PV mode-B prove-or-disprove invariants (forward design).  This DEFAULT
    \* harness is mode-A (GoalMode = "prose"), so NoHumanInModeB /
    \* ModeBConfiguredTargetsEmpty hold vacuously; SoundnessFloor /
    \* NotBothSidesClosed / CoverageOnLiveOnly / DecidedResolves still bind.
    ModeBConfiguredTargetsEmpty
    SoundnessFloor
    NotBothSidesClosed
    NoHumanInModeB
    UnderModelAssumptionLifecycleContract
    CoverageOnLiveOnly
    DecidedResolves

PROPERTIES
    AuditSoleFlipperProp

\* Bounded exhaustive search.  See SupervisorCoreSim.tla for SimNodes /
\* SimTargets / SimMaxCycle bounds — those drive the state-space size.
