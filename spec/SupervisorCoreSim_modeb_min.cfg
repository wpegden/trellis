SPECIFICATION Spec

CHECK_DEADLOCK
    FALSE

\* Minimal mode-B GREEN safety check (sim-only).  Confirms the PV mode-B
\* safety invariants hold on the 1-node / 1-disprove-target configuration —
\* the configuration whose ONLY route to completion is the disprove side.
INVARIANTS
    TypeOK
    SoundnessFloor
    NotBothSidesClosed
    NoHumanInModeB
    DecidedResolves

\* Bait reachability witnesses (run each as its own INVARIANTS-of-one cfg; a
\* VIOLATION is the witness):
\*   NeverProofPhase        — proof_formalization reachable in mode-B
\*   C1NeverFlipped         — audit Prove->Disprove flip reachable
\*   C1NeverDisproveClosed  — disprove side closes via the axiom-floor gate
\*   DoneNeverComplete      — full run to Done (longer; warrants a longer sim)
\*
\* Invoke under -simulate (sim-only):
\*   java -jar tla2tools.jar -simulate num=80000000 -depth 90 -seed 1 \
\*       -config SupervisorCoreSim_modeb_min.cfg SupervisorCoreSim_modeb_min.tla
