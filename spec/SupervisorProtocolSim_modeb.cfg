SPECIFICATION Spec

\* Mode-B (GoalMode "lean") PV prove-or-disprove instance: configured-target
\* bucket empty, one challenge target whose default Prove side is FALSE
\* (ChallengeTruth "disprove"), so completion is reachable only after the
\* StuckMathAudit lane flips the polarity. Exercises the prove-or-disprove
\* invariants NON-vacuously. Sim-only (RAM-heavy big spec). See SPEC_TODO §0.

INVARIANTS
    TypeOK
    PendingTaskConsistent
    HumanGateMatchesState
    InFlightCycleBounded
    PhaseDormancyContract
    StageOwnership
    GlobalBlockersExhaustive
    CleanupHasNoBlockers
    CleanupDoneTerminal
    \* PV challenge-coverage (Step 1) + prove-or-disprove (Step 2).
    ModeBConfiguredTargetsEmpty
    CompleteRequiresChallengeCoverage
    CoverageOnLiveOnly
    SoundnessFloor
    NotBothSidesClosed
    NoHumanInModeB
    UnderModelAssumptionLifecycleContract
    DecidedResolves

PROPERTIES
    AuditSoleFlipperProp
