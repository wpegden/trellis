SPECIFICATION DeliberatelyBuggySpec

INVARIANTS
    HumanGateMatchesState

\* Sanity check: a deliberately corrupted Init violates the
\* HumanGateMatchesState invariant from the very first state.
\* See SupervisorCoreSim_bugcheck.tla.
