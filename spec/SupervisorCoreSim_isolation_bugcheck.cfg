SPECIFICATION CrossTargetBuggySpec

INVARIANTS
    CrossTargetFormalIsolation

\* Phase IV step 12 teeth check (THROWAWAY, expected-FAIL): a non-uniform
\* nodeTarget across the n1 -> n2 formal import violates
\* CrossTargetFormalIsolation on the very first state. See
\* SupervisorCoreSim_isolation_bugcheck.tla. NOT part of the green suite.
