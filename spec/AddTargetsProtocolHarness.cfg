SPECIFICATION HarnessSpec

\* The harness deadlocks by design once the follow-on StartCycle has run
\* (its Next contains only the add-targets action + StartCycle).
CHECK_DEADLOCK
    FALSE

INVARIANTS
    TypeOK
    PhaseDormancyContract
    CleanupDoneTerminal
    StageOwnership
    GlobalBlockersExhaustive
    StuckMathAuditLatchWellFormed
    HumanGateMatchesState
    QuiescentLiveEqualsCommitted
    PendingTaskConsistent
    CoarseAnchorWellFormed
    CleanupAuditRoundBound
    ApprovedConfiguredTargetsPinned
    WindowMatchesRevivalShape
