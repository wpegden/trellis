import
  /- Legal whitespace and a comment inside the import command. -/
  Tablet.MultilineDep

/-! The documentation may mention `import Tablet.NotARealDependency`. -/
theorem MultilineConsumer : True := True.intro
