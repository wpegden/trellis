The kernel-authored contract and rubric are authoritative. Render exactly one verdict per node on the frontier (Pass / Fail / NotDoneYet via the schema above). Read the configured paper file directly from disk at `{{paper_tex_path}}` (path relative to repo root) — the kernel does not pre-extract paper content; that's your job.

Do not switch into Lean-vs-TeX node correspondence; that is the corr lane's responsibility on a later cycle. Your job is `.tex`-vs-paper at the node level.
