Note: the `Preamble` node is normally routed to the target-level paper-faithfulness lane, not this one. If `Preamble` is on the frontier of this request, treat each definition item in `Preamble.tex` as an independent claim (the file declares multiple definitions); judge each against the paper independently. When a specific item fails, use `verdicts[].node = "Preamble[<item-index>]"` matching the corr-lane preamble convention so the kernel can localize the fail.
