## Kernel-authored substantiveness frontier

{{request_summary_json}}

## Kernel-authored substantiveness paper-basis inputs

The following object lists, for each node on the frontier, the paths to its `.tex` and `.lean` files (relative to the repo root) and the names of nodes that import it (so you can judge how downstream usability is affected by a weakened or shifted statement). Use this to locate node content; read the `.tex` files directly via `read_files`.

{{node_paper_basis_inputs_json}}

## Previous own findings (per-node, this lane)

If non-empty, these are your lane's verdicts on these nodes from the immediately-preceding Paper request — typically because some nodes returned `NotDoneYet` and the kernel re-issued. Use as context, but evaluate the *current* `.tex` content.

{{previous_own_findings_json}}
