This lane is revisiting node-level correspondence after one of your earlier findings. See `CORRESPONDENCE.md` (inlined elsewhere in this prompt) for the canonical rubric.

Check whether the node statements now resolve the specific Lean-vs-TeX alignment failures you flagged before. Do not clear a node just because surrounding files changed; look for the actual statement-level fix. You must still emit a per-node verdict for every node in the verify list — silence on a node defaults to Fail.
