`Preamble` is in this correspondence frontier.

For `Preamble`, use the special one-way support rule from the contract. Judge only whether the structured statement items exposed by `Preamble.tex` are supported by the Lean interface, not whether `Preamble.tex` looks like an ordinary theorem node.
