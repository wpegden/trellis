# Challenge-covering nodes

This run also carries prescribed challenge declarations whose Lean text is fixed by an external benchmark and enforced byte-for-byte by the kernel. For these nodes correspondence repairs should only direct changes to TeX statements.
