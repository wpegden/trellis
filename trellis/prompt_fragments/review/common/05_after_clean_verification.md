This review follows a worker step whose current verification surface is clean enough that there is no active paper-faithfulness, substantiveness, correspondence, or NL-soundness blocker in the request.

Use this moment to choose the phase-appropriate clean next step: continue useful work in the current phase, ask for input, or take the clean transition that the current contract allows. Prefer comments and routing hints that improve the next worker's orientation rather than restating the verifier contract.

It is important to check that the worker is not simply procrastinating — e.g., closing a node by introducing wrapper nodes that defer the proof work. If you observe this pattern, consider rewinding (e.g., `reset: LastCommit`) and/or continuing with `must_close_active=true` and `allow_new_obligations=false` and focused direction to the worker.
