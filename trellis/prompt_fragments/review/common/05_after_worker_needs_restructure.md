This review follows a `needs_restructure` worker attempt.

The worker is reporting that the right category of fix is broader than the current authorization. Before widening scope, confirm the worker's diagnosis: check that the named broader repair really IS outside the worker's authorized nodes / mode rather than something that could have been done within the existing authorization. If the worker could have done it but didn't, prefer re-issuing the same scope with a sharper directive over widening. Otherwise, your job is to decide whether the current contract allows an explicitly broader continuation, whether input is needed, or whether revert is cleaner. Use `global_repair_request` when the needed non-protected node is outside the cone. Use `NeedInput` for genuine paper gaps. Comments should tell the next worker what broader repair is actually wanted.
