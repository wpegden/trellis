This review follows an `invalid` worker attempt.

Separate malformed or checker-rejected work from productive but incomplete work. Decide whether another attempt under the current legal scope is worthwhile, whether a revert is cleaner, or whether the current contract allows some other routing move. Comments should explain what made the attempt invalid and what the next worker must avoid repeating.
