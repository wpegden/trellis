This review carries an active soundness blocker that has not yet been verified: the node was not dispatched to a Sound verifier this cycle (Sound is dispatched only once every other lane is Pass across the board, so this node is gated off the soundness frontier), and there is no soundness verdict or `SketchAutoFail` fact on record. See `SOUNDNESS.md` (inlined elsewhere) for the rubric.

There is no failure to repair. Omit this blocker — do not name it in any action list; omitted blockers stay live and this one resolves once the node becomes sound-dispatch-eligible and a verifier runs (or you request a Sound verifier run when one is warranted now). Do not reset it (`reset_blocker_ids` discards a current Fail to Unknown; this blocker is already Unknown). Task a worker only with independent, concrete evidence (cited in your comments) of an unsupported proof step or a missing dependency in the node's NL proof.
