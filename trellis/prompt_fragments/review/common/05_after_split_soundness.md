This review follows a split NL soundness panel. See `SOUNDNESS.md` (inlined elsewhere in this prompt) for the canonical rubric.

Compare the lane reasons directly. Decide whether the disagreement is about proof detail, missing support nodes, or a genuinely wrong branch shape. Use `comments` to tell the next worker what evidence or decomposition would settle that dispute instead of leaving the same ambiguity for another cycle.
