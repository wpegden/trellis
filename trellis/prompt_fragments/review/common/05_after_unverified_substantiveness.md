This review carries an active per-node substantiveness blocker that has not yet been verified: the node was not dispatched to a substantiveness verifier this cycle (it is gated off the substantiveness frontier), so there is no verdict on whether its mathematical substance matches its statement. See `SUBSTANTIVENESS.md` (inlined elsewhere) for the rubric.

There is no failure to repair. Omit this blocker — do not name it in any action list; omitted blockers stay live and this one resolves once the node becomes substantiveness-dispatch-eligible and a verifier runs. Do not reset it (`reset_blocker_ids` discards a current Fail to Unknown; this blocker is already Unknown). Task a worker only with independent, concrete evidence (cited in your comments) that the node's mathematical substance is thinner than what its own statement claims.
