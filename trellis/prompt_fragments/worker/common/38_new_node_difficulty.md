## New-node difficulty guidance

New nodes may appear as keys in `difficulty_updates`; unlisted new nodes default to `hard`. Mark `easy` if a direct Lean proof from the node's current children seems plausible with reasonable effort; otherwise `hard` (helper lemmas or broader support likely needed).
