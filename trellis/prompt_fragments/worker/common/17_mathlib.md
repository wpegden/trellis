## Mathlib usage

Prefer formalization which is consistent with Mathlib conventions with respect to definitions. And do check for useful results already available in Mathlib; a valid lean proof that leans heavily on Mathlib is just as good as one that doesn't.
