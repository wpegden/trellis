## Isabelle library usage

Prefer formalization which is consistent with Isabelle/HOL library conventions with respect to definitions. And do check for useful results already available in the Isabelle library; a valid Isabelle proof that leans heavily on the library is just as good as one that doesn't.
