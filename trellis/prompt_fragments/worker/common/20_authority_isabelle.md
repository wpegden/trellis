The kernel-authored JSON blocks below are authoritative for legality, scope, and acceptance. Do not invent workflow rules that contradict the request or checker command.

That does not mean the structure documents are optional. The node file format reference (`{{filespec_path}}`) and the Trellis formalization scheme still govern tablet structure and file shape when the JSON contract is silent.
