This proof-formalization task includes correspondence repair on one or more helper nodes you (or an earlier worker) added. See `CORRESPONDENCE.md` (inlined elsewhere in this prompt) for the canonical rubric. The blocked nodes are listed under `blockers` in the kernel-authored request above, with `kind: "NodeCorr"`.

Note that when bringing Lean and TeX statements into correspondence, the guiding principle for most nodes about which file needs to change is that the Lean statement ultimately needs to be both sufficient for use by its importers and ultimately provable; thus to complete this task well you should look at the nodes importing this node, and think about what their proofs need, as well as thinking about how this node follows from its own imports. The exception is nodes covering paper targets; for these, the NL TeX statements are independently required to be paper-faithful (and such nodes may well have no importers) so that the TeX in these cases is often controlling.
