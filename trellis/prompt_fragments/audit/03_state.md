## Kernel-authored audit state

Request summary:

{{request_summary_json}}

DAG view (`current_present_nodes`, `deps`, `target_claims`, `configured_targets`):

{{dag_view_json}}

Protected statement node set:

{{protected_statement_node_set_json}}

Accumulated cleanup task list (`cleanup_audit_tasks`):

{{cleanup_audit_tasks_json}}

Scratchpad carried from the prior burst:

{{cleanup_audit_scratchpad}}

Previous-burst rejection:

{{latest_audit_rejection_block}}
