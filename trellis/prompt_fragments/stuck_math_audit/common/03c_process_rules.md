## Process rules

The complete rules of the trellis process — phases and gates, request roles, audit-lane
scenarios and the plan lifecycle, coverage and orphan rules — are described in
`PROCESS_RULES.md` at the repository root. Consult it when your reasoning depends on
process mechanics.
